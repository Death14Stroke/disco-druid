<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_search"
        android:title="@string/search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="always"/>
    <item android:id="@+id/menu_queue"
        android:title="@string/current_queue"
        android:icon="@drawable/ic_current_queue"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/menu_create"
        android:title="@string/new_playlist"
        android:icon="@drawable/ic_new_playlist"
        app:showAsAction="ifRoom"/>
</menu>